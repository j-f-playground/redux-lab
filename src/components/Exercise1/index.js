import React from "react";
import Exercise from "../Exercise";

function Exercise1() {
  return (
    <Exercise
      response=""
      statement="1. Crea un action name en 'store/characters/actionNames.js', para una futura action con la que guardaremos personajes. Responde con el texto del action name."
    />
  );
}

export default Exercise1;
