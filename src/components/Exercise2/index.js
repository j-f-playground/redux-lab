import React from "react";
import Exercise from "../Exercise";

function Exercise2() {
  return (
    <Exercise
      response=""
      statement="2. Crea un action en 'store/characters/actions.js' para guardar personajes. Los personajes los recibirá por parámetros y utilizaremos el action name existente como type. Responde con el código del action."
    />
  );
}

export default Exercise2;
