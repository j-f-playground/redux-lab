import React from "react";
import Exercise from "../Exercise";

function Exercise4() {
  return (
    <Exercise
      response=""
      statement="4. Crea un botón que ejecute la acción creada anteriormente utilizando useDispatch. Como parámetro, envíale un array de objetos cualquiera. Al ejecutarlo, en la consola de Redux tienes que observar que se ha ejecutado el action correctamente y que el store ha sido afectado. Responde con el código de la función que ejecuta el botón."
    />
  );
}

export default Exercise4;
